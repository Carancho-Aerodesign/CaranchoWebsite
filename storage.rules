rules_version = '2';

// Allow read/write for authenticated users. Tie future logic to Firestore if needed.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
